# servermonitor 项目交接文档

> 交接日期：2026-09-14
> 当前版本：`0.1.20`
> 仓库：https://github.com/qsbb/servermonitor
> 最新 commit：`de3d164`

---

## 1. 项目一句话说明

`servermonitor` 是一个 **TRSS-Yunzai 插件**，用来把多台服务器（Linux / Windows / macOS / Docker）的 CPU、GPU、内存、网络、磁盘、温度、功耗等状态，汇总渲染成一张图片，通过 `#服务器状态` 发给管理员。

采集方式为 **Push 模式**：每台被监控机器跑一个 agent，定时把 JSON 快照 POST 到 Yunzai 的一个 HTTP 路由。

---

## 2. 整体架构

```text
被监控服务器（agent.mjs）
        │  每 10s POST JSON
        │  Header: X-SM-Token
        ▼
TRSS-Yunzai :2536
  └─ Bot.express
       └─ POST /servermonitor/report   （server.js 注册路由）
              │
              ▼
         model.js  handleReport()
              │  校验 token / 限速 / 清洗数据
              ▼
         内存 state.records + data/snapshots.json
              │
              ▼
         #服务器状态 → buildStatusData() → art-template → Chromium 截图 → 发图
```

关键点：

- **不额外监听端口**，复用 Yunzai 的 `Bot.express`。
- 路由注册放在 `server.js`，并加 `Bot.express.skip_auth` / `quiet`，让插件自己的 `X-SM-Token` 生效，同时不刷日志。
- 状态数据是「内存态 + 定时落盘」，`data/snapshots.json` 只保存最近快照，用于重启后恢复。

---

## 3. 目录结构

```text
server-status-plugin/
├── index.js                  # 插件入口：命令规则、task 定时任务、渲染回复
├── model.js                  # 核心：状态管理、token、上报处理、渲染数据组装（最大文件）
├── config.js                 # config.yaml 读写、YAML 解析、配置互斥锁
├── server.js                 # HTTP 路由注册（/servermonitor/report）
├── local.js                  # 采集「Yunzai 本机」状态（复用 agent 逻辑的本地版）
├── resources/
│   ├── server_status.html    # 状态图模板（art-template）
│   └── status.css            # 状态图样式（曜石风格）
├── agent/
│   ├── agent.mjs             # 被监控机器上的采集 agent（systeminformation）
│   ├── package.json          # 仅依赖 systeminformation
│   ├── Dockerfile
│   └── README.md
├── scripts/
│   ├── install.sh            # 交互式安装器入口
│   ├── install.ps1
│   ├── install-plugin.sh     # 安装 Yunzai 插件（自动测速镜像）
│   ├── install.mjs           # install.sh 的 Node 实现
│   ├── install-agent-linux.sh
│   ├── install-agent-docker.sh
│   ├── install-agent-macos.sh
│   └── install-agent-windows.ps1
├── docs/
│   ├── 最终开发方案.md
│   ├── 优化与诊断方案.md
│   ├── 适配文档-第三方工具箱上报.md
│   └── Windows-GPU占用率修复说明.md
├── design/                   # UI 设计稿（option-a / option-b / 预览图，已 gitignore）
├── dist/                     # 打包出的 exe（已 gitignore）
├── config.example.yaml       # 配置模板
├── config.yaml               # 运行时配置（含 token，已 gitignore）
├── data/                     # snapshots.json / pending.json（已 gitignore）
├── 部署文档.md
├── 开发文档.md
├── 快速部署教程.md
└── README.md
```

---

## 4. 运行环境

| 项 | 要求 |
| --- | --- |
| Yunzai | TRSS-Yunzai v3.x（实测 v3.1.3） |
| Node.js | ≥ 18（agent 和插件都是 ESM） |
| 插件运行时依赖 | **无**（`package.json` 无 dependencies） |
| agent 依赖 | `systeminformation` |
| 渲染 | Yunzai 自带 puppeteer / Chromium |
| 默认端口 | 2536（Yunzai HTTP 端口） |

---

## 5. 插件侧：命令一览

定义在 `index.js` 的 `rule` 数组，`priority: 5000`。

| 命令 | fnc | 权限 | 说明 |
| --- | --- | --- | --- |
| `#服务器状态检查` | check | 管理员 | 插件加载 / 路由 / 配置 / 渲染自检 |
| `#服务器状态` | statusAll | 管理员 | 总览图 |
| `#服务器状态 <名称>` | statusOne | 管理员 | 单机详情 |
| `#服务器状态列表` | list | 管理员 | 文本列表（带序号） |
| `#服务器状态帮助` | help | 所有人 | 帮助 |
| `#服务器状态命令 <名称>` | command | master | 重新获取该机器的独立 token 和启动命令 |
| `#服务器状态令牌` | token | master | 已停用，返回迁移说明 |
| `#服务器状态待绑定` | pending | master | 查看待绑定 token（群聊拒绝） |
| `#服务器状态添加 <名称> [上报地址] [备注]` | add | master | 手动加服务器、生成独立 token，并可指定 agent 实际访问地址 |
| `#服务器状态绑定 <token>` | bind | master | 按 agent 上报名绑定 |
| `#服务器状态改名 <旧> <新>` | rename | master | 改名 |
| `#服务器状态删除 <名称>` | del | master | 按名称删除（兼容别名） |
| `#删除服务器 <名称\|序号>` | deleteTarget | master | 删除服务器；数字走二次确认，名称直接删除 |
| `#删除服务器` | deleteByIndex | master | 无参数时列出服务器 |
| `#确认删除服务器 <序号>` | confirmDelete | master | 二次确认删除 |
| `#服务器状态插件更新` | updatePlugin | master | `git pull --ff-only` 官方仓库 |

权限说明：

- 查看类命令 **rule 不设 permission**，在 fnc 内用 `_isAdmin()` 判断（`e.isMaster || admins.includes(user_id)`）。
- 管理类命令直接用 `permission: "master"`。
- `public_status: true` 时任何人可看状态图。

---

## 6. 定时任务

定义在 `index.js` 的 `task` 数组。

> ⚠️ **踩坑记录**：`task.fnc` 必须直接传**函数引用**，不能传字符串方法名。传字符串会报 `TypeError: i.fnc is not a function`（见 commit `6a8e4ba`）。

| 任务 | 频率 | 作用 |
| --- | --- | --- |
| servermonitor离线扫描 | `*/10 * * * * *` | 判定离线、发上下线提醒 |
| servermonitor快照落盘 | `*/30 * * * * *` | 把内存快照写入 `data/snapshots.json` |

---

## 7. 上报接口（最重要）

### 7.1 路由

- 方法：`POST`
- 路径：`/servermonitor/report`
- 别名：`/server-monitor/report`
- Header：`X-SM-Token: <token>`、`Content-Type: application/json`
- 体积上限：stock TRSS 全局 JSON 解析器默认 100 KB 先生效；插件 256 KB 为第二道防线
- 限速：单 token+IP `60 次/分钟`；单 IP `240 次/分钟`

### 7.2 请求体 schema

```json
{
  "v": 1,
  "name": "srv-02",
  "agent_ts": 1730000000000,
  "os":     { "platform": "linux", "distro": "Debian GNU/Linux", "release": "12", "arch": "x64", "hostname": "srv-02", "uptime": 123456 },
  "cpu":    { "model": "Ryzen 7 9700X", "cores": 16, "usage": 37.5, "temp": 62, "power": null },
  "gpus":   [ { "model": "RTX 3060", "usage": 55, "temp": 65, "memUsed": 2.4, "memTotal": 12, "power": 120 } ],
  "mem":    { "used": 8.2, "total": 16, "available": 7.8, "swapUsed": null, "swapTotal": null },
  "net":    { "iface": "eth0", "rxSec": 12.3, "txSec": 4.5, "rxTotal": 1024.5, "txTotal": 512.1 },
  "disks":  [ { "mount": "/", "used": 103.5, "total": 215.4 } ],
  "load":   [ 1.2, 1.1, 1.0 ]
}
```

约定：

- `v` 必须为 `1`，否则 `422 bad schema`。
- 单位：温度 ℃、容量 GB、速率 MB/s、`usage` 百分比。
- 数值必须是 number，不要传字符串。
- `null` 表示未知；`gpus` / `disks` 可为空数组。
- `name` 会被清洗：去掉 `<>&"'`\\`，空白转 `_`，截断 32 字符。
- `mem.available` 为可选 GB 字段；有合法值时面板按 `total - available` 计算压力，缺失时回退 `used`，因此旧 agent 和第三方工具仍兼容。

### 7.3 token 与绑定流程

共享 token 已停用，只支持独立 token：

1. **agent 生成 token，再回 Yunzai 绑定**
   - 安装器自动生成 `sm_` + 32 位十六进制 token，打印 `#服务器状态绑定 sm_xxx`。
   - 上报时插件先存 `pending`，返回 `202`，绑定后转正。
2. **Yunzai 先生成独立 token**
   - `#服务器状态添加 <名称> [上报地址] [备注]`，把 token 配到 agent；省略地址时使用 Yunzai 的 `server.url` 配置。
3. **旧共享 token 节点迁移**
   - 携带旧 `shared_token` 的上报返回 `401 shared token disabled`。
   - 存量自动注册记录仍保留独立 token，执行 `#服务器状态命令 <名称>` 取回并更新 agent。

### 7.4 返回码

| 状态 | 含义 |
| --- | --- |
| 200 | 成功，`{ ok:true, name, auto:false }` |
| 202 | 已存待绑定，返回 `bind` 命令 |
| 401 | token 无效 / 格式不合法 / 旧共享 token 已停用 |
| 413 | 请求体超过宿主上限（stock TRSS 全局 JSON 上限 100KB） |
| 422 | schema 错误 / 空快照 / token 过长 |
| 429 | 限速 / 待绑定队列已满 / 服务器数量达上限（64 台） |
| 503 | `report_enabled: false`，当前停止接收上报 |
| 500 | 服务端内部错误 |

---

## 8. agent 侧

### 8.1 环境变量

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `SM_NAME` | 无 | 服务器名，必须唯一 |
| `SM_TOKEN` | 自动生成 | 上报 token |
| `SM_REPORT_URL` | 无 | 上报地址 |
| `SM_INTERVAL` | 10 | 快采集周期（秒），最小 5 |
| `SM_SLOW_INTERVAL` | 30 | 慢采集周期（秒），≥ interval |
| `SM_TIMEOUT` | 5000 | 采集超时（ms） |
| `SM_DRY_RUN` | 无 | 只打印不发送 |
| `SM_ONCE` | 无 | 只上报一次 |

也支持配置文件 / 命令行参数（`agent.mjs --help`）。

### 8.2 采集策略

- **快采集**（`SM_INTERVAL`）：CPU 使用率、内存、网络速率。
- Linux 内存额外上报 `available`（对应 `MemAvailable`），服务端据此排除可回收 page cache；容器 cgroup 限额识别仍是后续事项。
- **慢采集**（`SM_SLOW_INTERVAL`）：磁盘、CPU 温度、GPU、CPU 功耗。
- 所有采集都走 `safe(fn, fallback, timeout)`，**单个指标卡住不会拖垮整体**。
- GPU 采集顺序：
  1. 直接调 `nvidia-smi`；
  2. 失败则退到 `systeminformation.graphics()`，现在会取 `utilizationGpu / temperatureGpu / memoryUsed / powerDraw`（commit `de3d164`）。

### 8.3 各平台安装位置

| 平台 | 安装目录 | 服务 |
| --- | --- | --- |
| Linux | `/opt/servermonitor/agent` | systemd `servermonitor-agent` |
| Docker | `/opt/servermonitor-docker` | docker compose |
| macOS | `/opt/servermonitor/agent` | launchd `com.servermonitor.agent` |
| Windows | NSSM 服务 | 服务名 `servermonitor-agent` |

Linux 配置文件：`/etc/systemd/system/servermonitor-agent.service`（环境变量写在 `Environment=` 行）。

---

## 9. 配置项（config.yaml）

```yaml
admins: []              # 额外管理员 QQ
servers: []             # 服务器列表 [{name, token, reportUrl?, note, createdAt}]
trusted_proxies: []     # 可信反向代理直连地址；默认只信任 TCP 直连
report_enabled: true    # false 时停止接收所有上报
page_size: 8            # 每张图显示数量，上限 16
offline_timeout: 30     # 超过多少秒判定离线
public_status: false    # true 时任何人可查看
include_local: true     # 是否包含 Yunzai 本机卡片
alert:
  enabled: true
  cooldown: 120
render:
  imgType: png
```

- `config.yaml` 含密钥，**已 gitignore，不要提交**。
- `config.js` 内有 **配置写入互斥锁**，避免并发写坏文件。
- 解析器只接受已知 key，拒绝非法 key；损坏时回退缓存。

---

## 10. 安全设计（不要回退）

| 机制 | 位置 | 说明 |
| --- | --- | --- |
| 限速 | model.js `allowReport` | 防刷 |
| body 上限 | server.js + TRSS 全局解析器 | stock TRSS 全局 JSON 上限 100KB 先生效；插件 256KB 检查为第二道防线 |
| 上报响应校验 | agent/agent.mjs `postSnapshot` | 必须返回 JSON `ok:true`，空响应/无效响应按失败重试 |
| 文件权限 | config.js / model.js | config.yaml、pending.json、agent 配置均 0600，data 目录 0700 |
| 磁盘 / GPU 子进程超时 | agent/agent.mjs | `df` / `lspci` 使用 timeout + SIGKILL，避免挂载点卡死堆积子进程 |
| 字段/数组上限 | model.js `sanitizeSnapshot` | disks≤16、gpus≤8、字符串截断 |
| token 定时安全比较 | `timingSafeEqualStrings` | 防时序攻击 |
| 共享 token 停用 | `handleReport` | 旧共享 token 返回 401 和迁移提示 |
| 待绑定防泛洪 | `handleReport` | 格式校验、来源限速、队列满拒收、写盘去抖 |
| 可信代理限速 | `resolveClientIp` | 默认只信任直连地址，避免 XFF 伪造 |
| 配置写锁 | config.js | 防并发写坏 |
| 待绑定表上限 | `MAX_PENDING_ENTRIES=100` | 防内存膨胀 |
| 服务器上限 | `MAX_SERVERS=64` | 防滥用，数量检查在写入队列内 |

> 卸载/禁用插件后必须重启 Yunzai，Express 路由不会自动注销。TRSS debug 日志会记录完整 `X-SM-Token`，不要长期开启。

---

## 11. 部署命令（新机器）

Yunzai 地址以 `http://192.168.5.88:2536` 为例：

```bash
# Linux
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-linux.sh) \
  srv-02 sm_xxx http://192.168.5.88:2536/servermonitor/report

# Docker
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  srv-02 sm_xxx http://192.168.5.88:2536/servermonitor/report

# macOS
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-macos.sh) \
  srv-02 sm_xxx http://192.168.5.88:2536/servermonitor/report
```

Windows：

```powershell
powershell -ExecutionPolicy Bypass -File install-agent-windows.ps1 `
  -Name srv-02 -Token sm_xxx `
  -ReportUrl http://192.168.5.88:2536/servermonitor/report
```

要点：

- 安装器会**自动测速 GitHub 镜像**并选最快的。
- **重复执行同一条命令 = 更新模式**，会保留 `SM_NAME / SM_TOKEN / SM_REPORT_URL`。
- 日志：`journalctl -u servermonitor-agent -f`。

---

## 12. 已知问题与踩坑记录

| 问题 | 根因 | 状态 |
| --- | --- | --- |
| `i.fnc is not a function` | `task.fnc` 传了字符串方法名 | 已修 `6a8e4ba`，改成函数引用 |
| Windows 无 GPU 占用率 | 兜底只取型号和显存 | 已修 `de3d164` |
| 共享 token 上报被 403 | 自动注册后 agent 仍用共享 token | 已停用共享 token，见下 |
| `#服务器状态插件更新` 401 | `origin` 带了失效认证 | 已修 `385774a`，改为直接拉官方仓库 |
| agent 重复上报被拒 | 自动注册时生成了独立 token，但 agent 仍用共享 token | 已停用共享 token，见下 |
| CPU 功耗 / 温度不准 | RAPL 符号链接、thermal 命名差异 | 已修 |
| systemd / Docker 更新丢环境变量 | 解析未处理引号 | 已修 |

### 共享 token 已废弃（已处理）

原问题：共享 token 首次自动注册后，agent 继续使用共享 token 会永久收到 `403`。

当前处理：直接停用共享 token。

- 携带旧 `shared_token` 的上报返回 `401 shared token disabled` 和迁移提示。
- `config.js` 不再生成、不再写入 `shared_token`；旧值只用于识别。
- 存量自动注册记录保留其独立 token，可用 `#服务器状态命令 <名称>` 取回并更新 agent。
- 新节点统一走“独立 token + 绑定”流程。

---

## 13. 本地开发与验证

```bash
cd server-status-plugin

# 语法检查
node --check index.js
node --check model.js
node --check agent/agent.mjs
node --check config.js
node --check server.js

# 安装脚本语法
bash -n scripts/install-agent-linux.sh
bash -n scripts/install-agent-docker.sh
bash -n scripts/install-agent-macos.sh

# agent 干跑（不发送）
cd agent && node agent.mjs --dry-run
```

渲染预览：用本地 Puppeteer 的 Chromium 打开 `resources/server_status.html`。

发版：

```bash
# 改 package.json / agent/package.json / agent/package-lock.json / agent.mjs 里的版本号
git add -A && git commit -m "..." && git push origin main
# 打 tag
git tag v0.1.20 && git push origin v0.1.20
```

---

## 14. 交接清单

- [x] 源码与文档齐全
- [x] 一键安装器支持 Linux / Docker / macOS / Windows
- [x] agent 支持自动更新且保留配置
- [x] UI 为「曜石」风格（option B）
- [x] 安全加固（限速 / 上限 / 时序安全比较 / 写锁）
- [x] 共享 token 已停用，提供 `#服务器状态命令` 迁移入口
- [ ] Windows 非 NVIDIA 显卡的通用占用率采集未实现
- [ ] 单元测试缺失，目前只有语法检查和手工验证

---

## 15. 需要向接手人强调的三件事

1. **`name` 必须唯一**。这是现场最多的问题来源。
2. **`task.fnc` 只能传函数引用**，不要图省事改成字符串。
3. **`config.yaml` 别提交**，里面有 token。

---

## 16. 相关文档索引

| 文档 | 路径 |
| --- | --- |
| 快速部署 | `快速部署教程.md` |
| 详细部署 | `部署文档.md` |
| 开发细节 | `开发文档.md` |
| 第三方工具箱接入 | `docs/适配文档-第三方工具箱上报.md` |
| Windows GPU 修复说明 | `docs/Windows-GPU占用率修复说明.md` |
| 优化与诊断方案 | `docs/优化与诊断方案.md` |
| 最终开发方案 | `docs/最终开发方案.md` |
| agent 说明 | `agent/README.md` |
