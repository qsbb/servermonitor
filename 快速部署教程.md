# servermonitor 快速部署教程

这份教程覆盖四种部署方式：

1. Yunzai 插件安装
2. Linux 一键部署 agent
3. Docker / Docker Compose 部署 agent
4. Windows / macOS 常驻部署 agent

> 正确插件路径必须是 `plugins/servermonitor/index.js`。

## 0. 准备变量

把下面两个值替换成你的实际环境：

```bash
YUNZAI_DIR=/path/to/Yunzai
YUNZAI_URL=http://192.168.1.10:2536
```

- `YUNZAI_DIR`：TRSS-Yunzai 根目录
- `YUNZAI_URL`：其他服务器能访问到的 Yunzai 地址

## 1. 交互式一键安装器

Linux / macOS：

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install.sh)
```

如果已经 clone 仓库，也可以打开仓库目录运行：

```bash
./install.sh
```

Windows PowerShell：

```powershell
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install.ps1" -OutFile "$env:TEMP\servermonitor-install.ps1"
powershell -ExecutionPolicy Bypass -File "$env:TEMP\servermonitor-install.ps1"
```

如果已经 clone 仓库，也可以在仓库目录运行：

```powershell
.\install.ps1
```

打开后会让你选择：

1. 安装/更新 Yunzai 插件
2. 安装当前系统推荐的 agent
3. Linux systemd agent
4. Linux Docker Compose agent
5. Windows NSSM agent
6. macOS launchd agent
7. 只生成命令，不执行

安装 agent 时会交互输入：

- 服务器名称
- 上报 token
- Yunzai 机器 IP/域名
- Yunzai HTTP 端口，默认 `2536`
- 上报路径，默认 `/servermonitor/report`

### 1.1 2536 端口说明

`2536` 通常是 TRSS-Yunzai 的 HTTP 服务端口。`servermonitor` 复用这个 HTTP 服务，只新增一个路径：

```text
POST /servermonitor/report
```

这和 OneBotV11 使用同一个端口时属于同一个 HTTP 服务里的不同路径，不是两个程序抢同一个端口。agent 只要访问：

```text
http://YUNZAI_IP:2536/servermonitor/report
```

即可上报。

### 1.2 GitHub 克隆自动镜像加速

一键安装脚本会自动测试多个 GitHub 加速节点，选择最快的一个克隆源码；克隆失败还会自动重试。

如果本机已经安装过 agent，交互安装器会先显示已有配置，并让你选择：

```text
1. 更新并保留现有配置（推荐）
2. 重新配置
```

选择更新时会跳过名称/token/上报地址询问；底层脚本也会自动进入更新模式，只更新代码并重启服务。

日志类似：

```text
[servermonitor-agent] testing git mirror: https://github.com/qsbb/servermonitor.git
[servermonitor-agent] git mirror failed/timeout: https://github.com/qsbb/servermonitor.git
[servermonitor-agent] testing git mirror: https://ghfast.top/https://github.com/qsbb/servermonitor.git
[servermonitor-agent] git mirror ok: https://ghfast.top/https://github.com/qsbb/servermonitor.git 820ms
[servermonitor-agent] selected REPO_URL=https://ghfast.top/https://github.com/qsbb/servermonitor.git
```

常用环境变量：

```bash
AUTO_GIT_MIRROR=1                 # 开启自动测速
REPO_MIRRORS=...                  # 自定义逗号分隔的镜像列表
GIT_MIRROR_PROBE_TIMEOUT=5        # 单个节点探测超时秒数
GIT_CLONE_ATTEMPTS=3              # 克隆重试次数
GIT_CLONE_TIMEOUT=300             # 单次克隆超时秒数
```

手动指定仓库地址时会跳过测速：

```bash
REPO_URL=https://ghfast.top/https://github.com/qsbb/servermonitor.git \
  bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-plugin.sh) /path/to/Yunzai
```

## 2. 安装 Yunzai 插件

### 2.1 一键安装插件

在 Yunzai 所在机器执行：

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-plugin.sh) /path/to/Yunzai
```

或使用环境变量：

```bash
YUNZAI_DIR=/path/to/Yunzai bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-plugin.sh)
```

### 2.2 手动安装插件

```bash
cd "$YUNZAI_DIR"
# 首次安装
git clone https://github.com/qsbb/servermonitor.git plugins/servermonitor
# 更新已有安装（保留 config.yaml 和 data/）
cd plugins/servermonitor && git pull --ff-only
ls plugins/servermonitor/index.js
```

如果出现下面这种路径，说明装错了：

```text
plugins/servermonitor/servermonitor/index.js
```

正确路径是：

```text
plugins/servermonitor/index.js
```

### 2.3 重启 Yunzai

```bash
cd "$YUNZAI_DIR"
pnpm start
```

如果你用 systemd / pm2 / screen / docker 管理 Yunzai，就按你的原方式重启。

### 2.4 验证插件加载

在 QQ 里发送：

```text
#服务器状态检查
```

能看到 `【servermonitor】自检` 就说明插件已加载。

然后发送：

```text
#服务器状态
```

未添加远程服务器时，会显示 Yunzai 本机卡片 `本机`。

## 3. 上报 token 与绑定方式

上报 token 是 agent 上传数据时使用的密钥。现在支持两种方式：

### 3.1 推荐：部署机器生成 token，再回到 Yunzai 绑定

在被监控机器运行一键安装器时，token 可以留空。脚本会自动生成 token，并在最后输出绑定命令：

```text
[servermonitor-agent] token: sm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
[servermonitor-agent] bind in Yunzai private chat: #服务器状态绑定 sm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

看到一次上传日志后，把这条绑定命令复制到 Yunzai 主人私聊里发送即可：

```text
#服务器状态绑定 sm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

Yunzai 会从子服务器上报里读取机器名称；之后该机器会用自己生成的 token 上报。需要修改显示名称时发送：`#服务器状态改名 <旧名> <新名>`。如果提示还没收到该 token 的上报，等 agent 输出一次 uploaded 日志后再绑定。

### 3.2 可选：Yunzai 生成共享 token

主人私聊机器人：

```text
#服务器状态令牌
```

机器人会返回共享 token。所有服务器可共用这个 token，并按 `--name` / `SM_NAME` 自动注册。

### 3.3 可选：Yunzai 手动生成单机 token

主人私聊机器人：

```text
#服务器状态添加 web-01
```

机器人会为 `web-01` 生成独立 token 和 agent 启动命令。

下面部署 agent 时最少只需要两个值：

```text
NAME=web-01
REPORT_URL=http://YUNZAI_IP:2536/servermonitor/report
```

如果 token 留空，部署脚本会自动生成并提示你去 Yunzai 绑定。

## 4. Linux 一键部署 agent（推荐）

在被监控的 Linux 服务器执行：

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-linux.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

这个脚本会自动完成：

- clone 最新 agent
- 安装 npm 依赖
- 写入 systemd 服务
- 设置开机自启
- 立即启动 agent

查看状态：

```bash
systemctl status servermonitor-agent
```

查看日志：

```bash
journalctl -u servermonitor-agent -f
```

## 5. Docker 一键部署 agent

适合想用容器管理 agent 的 Linux 服务器。

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

这个脚本会自动完成：

- clone 仓库到 `/opt/servermonitor-docker`
- 写入 `.env`
- 自动测速 Node 基础镜像节点并选择最快的 `NODE_IMAGE`
- 构建 Docker 镜像
- 使用 `docker compose` 启动 agent

查看日志：

```bash
cd /opt/servermonitor-docker
docker compose -f docker-compose.agent.yml logs -f
```

停止：

```bash
cd /opt/servermonitor-docker
docker compose -f docker-compose.agent.yml down
```

### 5.1 Docker 镜像节点测速

Docker 一键脚本会对多个 Node 基础镜像地址执行 manifest 探测，选择响应最快的地址写入 `.env`：

```dotenv
NODE_IMAGE=docker.1ms.run/library/node:18-bookworm-slim
```

你也可以手动指定：

```bash
NODE_IMAGE=node:18-bookworm-slim \
  sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

关闭自动测速：

```bash
AUTO_NODE_IMAGE=0 sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

可调参数：

```bash
NODE_IMAGE_CANDIDATES="node:18-bookworm-slim,docker.1ms.run/library/node:18-bookworm-slim,docker.m.daocloud.io/library/node:18-bookworm-slim"
NODE_IMAGE_PROBE_TIMEOUT=8
NODE_IMAGE_PRE_PULL=1
```

### 5.2 手动 Docker Compose 部署

```bash
mkdir -p /opt/servermonitor-docker
cd /opt/servermonitor-docker
git clone https://github.com/qsbb/servermonitor.git .
cp .env.agent.example .env
```

编辑 `.env`：

```dotenv
SM_NAME=web-01
SM_TOKEN=sm_xxx
SM_REPORT_URL=http://192.168.1.10:2536/servermonitor/report
SM_INTERVAL=10
SM_SLOW_INTERVAL=30
SM_TIMEOUT=5000
```

启动：

```bash
docker compose --env-file .env -f docker-compose.agent.yml up -d --build
```

### 5.3 Docker 注意点

- Docker 部署主要适合 Linux
- 宿主机 CPU / 内存 / 网络大多可采集
- 温度、功耗、GPU 指标依赖宿主机暴露的 `/sys`、host pid/network、NVIDIA runtime
- 需要 NVIDIA GPU 时，安装 `nvidia-container-toolkit` 后在 `docker-compose.agent.yml` 中启用：

```yaml
gpus: all
```

宿主机指标最完整的方式是 Linux systemd 原生部署。

## 6. Linux 手动部署 agent

```bash
mkdir -p /opt/servermonitor
cd /tmp
rm -rf servermonitor-src
git clone https://github.com/qsbb/servermonitor.git servermonitor-src
cp -r /tmp/servermonitor-src/agent /opt/servermonitor/agent
cd /opt/servermonitor/agent
npm install
```

采集测试，不上传：

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxx \
  --report-url http://192.168.1.10:2536/servermonitor/report \
  --dry-run
```

上传一次：

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxx \
  --report-url http://192.168.1.10:2536/servermonitor/report \
  --once
```

持续运行：

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxx \
  --report-url http://192.168.1.10:2536/servermonitor/report \
  --interval 10 \
  --slow-interval 30
```

## 7. Windows 一键部署 agent

前置条件：

- Windows 已安装 Node.js 18+
- Windows 已安装 Git
- 使用管理员 PowerShell

管理员 PowerShell 执行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process -Force
irm https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-windows.ps1 -OutFile $env:TEMP\install-agent-windows.ps1
powershell -ExecutionPolicy Bypass -File $env:TEMP\install-agent-windows.ps1 `
  -Name "win-01" `
  -ReportUrl "http://192.168.1.10:2536/servermonitor/report"
```

这个脚本会自动完成：

- clone 最新 agent 到 `C:\servermonitor\agent`
- 执行 `npm install --omit=dev`
- 下载或复用 NSSM
- 注册 Windows 服务 `servermonitor-agent`
- 写入环境变量
- 启动服务

查看服务状态：

```powershell
nssm status servermonitor-agent
```

查看日志：

```powershell
Get-Content C:\servermonitor\agent.log -Wait
Get-Content C:\servermonitor\agent.err.log -Wait
```

### 7.1 Windows 手动部署

```powershell
git clone https://github.com/qsbb/servermonitor.git C:\servermonitor\src
Copy-Item -Recurse -Force C:\servermonitor\src\agent C:\servermonitor\agent
cd C:\servermonitor\agent
npm install
node agent.mjs --name win-01 --token sm_xxx --report-url http://192.168.1.10:2536/servermonitor/report --once
```

注册服务：

```powershell
nssm install servermonitor-agent "C:\Program Files\nodejs\node.exe" "C:\servermonitor\agent\agent.mjs"
nssm set servermonitor-agent AppDirectory "C:\servermonitor\agent"
nssm set servermonitor-agent AppEnvironmentExtra `
  "SM_NAME=win-01" `
  "SM_TOKEN=sm_xxx" `
  "SM_REPORT_URL=http://192.168.1.10:2536/servermonitor/report" `
  "SM_INTERVAL=10" `
  "SM_SLOW_INTERVAL=30"
nssm start servermonitor-agent
```

## 8. macOS 一键部署 agent

前置条件：

- macOS 已安装 Node.js 18+
- macOS 已安装 Git

执行：

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-macos.sh) \
  mac-01 \
  http://192.168.1.10:2536/servermonitor/report
```

这个脚本会自动完成：

- clone 最新 agent 到 `/opt/servermonitor/agent`
- 执行 `npm install --omit=dev`
- 写入 launchd 配置 `/Library/LaunchDaemons/com.servermonitor.agent.plist`
- 设置开机自启
- 启动 agent

查看日志：

```bash
tail -f /var/log/servermonitor/agent.log /var/log/servermonitor/agent.err.log
```

查看 launchd 状态：

```bash
launchctl print system/com.servermonitor.agent
```

### 8.1 macOS 手动部署

```bash
sudo mkdir -p /opt/servermonitor
cd /tmp
rm -rf servermonitor-src
git clone https://github.com/qsbb/servermonitor.git servermonitor-src
sudo cp -r /tmp/servermonitor-src/agent /opt/servermonitor/agent
cd /opt/servermonitor/agent
sudo npm install --omit=dev
node agent.mjs --name mac-01 --token sm_xxx --report-url http://192.168.1.10:2536/servermonitor/report --once
```

常驻运行建议使用上面的一键脚本生成 launchd 配置。

## 9. 验证多机状态

部署任意 agent 后，在 QQ 发送：

```text
#服务器状态
```

应看到：

- `本机`
- `web-01` / `win-01` / `mac-01`
- 每台机器一张独立卡片

查看列表：

```text
#服务器状态列表
```

查看单台：

```text
#服务器状态 web-01
```

## 10. 常见问题

### 10.1 发命令没反应

检查：

```bash
ls /path/to/Yunzai/plugins/servermonitor/index.js
```

然后在 QQ 发：

```text
#服务器状态检查
```

如果群里没回应，私聊测试或在群里 at 机器人：

```text
@机器人 #服务器状态检查
```

### 10.2 agent 上传失败

在服务器上执行：

```bash
curl -i -X POST \
  -H "X-SM-Token: <token>" \
  -H "Content-Type: application/json" \
  -d '{"v":1,"name":"probe"}' \
  http://192.168.1.10:2536/servermonitor/report
```

然后检查：

- `REPORT_URL` 是否为服务器可访问地址
- `TOKEN` 是否和 `#服务器状态添加` 返回的一致
- 防火墙是否放行 Yunzai HTTP 端口
- 日志里是否有 `uploaded`

### 10.3 Docker 看不到 GPU / 温度 / 功耗

检查：

- 是否安装 `nvidia-container-toolkit`
- 是否启用 `gpus: all`
- 宿主机 `/sys/class/thermal`、`/sys/class/powercap` 是否存在
- 需要完整宿主机硬件指标时改用 Linux systemd 原生部署

## 11. 升级

```bash
cd /path/to/Yunzai
cd plugins/servermonitor
git pull --ff-only
```

重启 Yunzai 后发送：

```text
#服务器状态检查
```
