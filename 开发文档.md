# TRSS-Yunzai 多服务器统一状态监控插件开发文档 v3

> 主方案：**Yunzai 作为中心收数端**，其他服务器部署 agent，**agent 主动上传状态到 Yunzai 暴露的专用接口**。
> 目标：用户发送 `#服务器状态`，Bot 返回一张图；默认包含 Yunzai 本机状态，并显示所有已注册服务器的 CPU / GPU / 内存 / 网速 / 磁盘 / 温度 / 功耗 / 型号 / 在线状态。
> 版本说明：本版已经把架构收敛为**单主线**，不再把 pull/SSH 当主方案；它们最多作为未来扩展。

---

## 0. 本版修订说明

这一版只保留一条主线：

```text
被监控服务器上的 agent   →   主动 POST 状态   →   Yunzai 专用接口
                                         ↓
                               内存快照 / 离线判定 / 渲染出图
```

和前一版相比，本版做了三件关键调整：

1. **架构收敛**：明确选定 **push 模式** 作为主方案。
2. **安全边界更清晰**：Yunzai 只暴露一个专用上报接口，不暴露管理能力。
3. **实现更聚焦**：文档里所有命令、协议、渲染、告警都围绕“上报 → 缓存 → 出图”展开。
4. **本机兜底**：未注册服务器时，`#服务器状态` 直接采集 Yunzai 本机；添加远端服务器后，总览可继续包含本机卡片。

---

## 1. 第一性原理：这个系统真正要解决什么

用户真正要的不是“协议”，而是这句话：

> **管理员发一条命令，就能拿到一张可信、及时、看得懂的全服务器状态图。**

把它拆开，系统只剩六个必须成立的环节：

1. **采集**：每台服务器自己读本机指标。
2. **上传**：把采到的指标主动发给 Yunzai。
3. **缓存**：Yunzai 保存每台服务器的最新快照。
4. **判定**：Yunzai 判断谁在线、谁离线、谁异常。
5. **渲染**：把快照变成图。
6. **发送**：把图通过 QQ 发出去。

也就是说：

- agent 的职责是“**测量并上报**”；
- Yunzai 的职责是“**收数、存数、判定、展示**”。

这就是第一性原理下最稳的分工。

---

## 2. 为什么选“Yunzai 暴露一个上报端口 + agent 主动上传”

这是监控系统里最经典的一类架构之一，关键优点是：

### 2.1 安全性

- agent 只需要一个**只读上报 token**；
- 不需要 SSH 私钥/密码；
- 不需要给 Bot shell 权限；
- 被攻击后，最多伪造状态图，不是远程执行。

### 2.2 可维护性

- 所有机器都输出同一个 JSON 契约；
- Yunzai 侧只维护一套存储/渲染逻辑；
- 换采集方式时，不需要动渲染层。

### 2.3 占用

- 每 5～10 秒上传一次，流量很小；
- 只存最新快照时，磁盘占用几乎可忽略；
- 真正的成本主要是 agent 常驻进程，不是上报动作本身。

### 2.4 适用性

这套模式尤其适合：
- 公网 VPS；
- Bot 可以被这些服务器访问到；
- 你希望监控端逻辑尽量简单。

### 2.5 需要满足的前提

> **Yunzai 必须对这些 agent 可达。**

如果 Yunzai 在公网 VPS 上，最简单；
如果 Yunzai 在家里 NAT 后面，就需要反向代理 / 端口映射 / tunnel（如 frp、Cloudflare Tunnel、Tailscale 等）。

---

## 3. 需求清单

| 编号 | 指标 | 说明 |
|---|---|---|
| R1 | CPU 占用 | 百分比 |
| R2 | CPU 型号 | 如 Xeon / Ryzen |
| R3 | CPU 温度 | 取不到则显示 `—` |
| R4 | GPU 占用 | NVIDIA 优先 |
| R5 | GPU 型号 | 必须显示 |
| R6 | GPU 温度 / 显存 / 功耗 | NVIDIA 优先 |
| R7 | 内存占用 / 总量 | 必须显示 |
| R8 | 网速 | 实时上下行 |
| R9 | 磁盘占用 / 容量 | 每块盘或系统盘汇总 |
| R10 | 功耗 | CPU / GPU / 整机可选 |
| R11 | 系统信息 | OS / 内核 / 架构 / uptime |
| R12 | 在线状态 | 在线 / 离线 / 数据时间 |
| R13 | 多平台 | Linux / Windows / macOS |

### 可用性等级

- **S**：开箱即用。
- **A**：主流场景可用，但有前置条件。
- **B**：可用但依赖特定软件/权限。
- **C**：通常取不到，UI 以 `—` 体现。

---

## 4. 经过调研后的架构决策

### ADR-1：为什么主方案是 push

因为你的最终目标是“**Yunzai 暴露一个端口，其他服务器主动上传**”。
这和 komari / nezha / ServerStatus 的主流思路一致：

- agent 在被监控机上运行；
- agent 主动连到中心端；
- 中心端只负责保存和展示。

这是最符合监控系统习惯的方向。

### ADR-2：为什么不用 SSH 做主方案

SSH 当然能做，但它的问题是：

- Bot 要保存所有服务器的 shell 凭据；
- 一旦 Bot 被攻破，风险比只读 token 大得多；
- shell 文本解析长期维护成本高；
- Windows / macOS 兼容性很差。

因此 SSH 只能算备用方案，不进主线。

### ADR-3：为什么只暴露一个专用接口

因为安全边界越清晰越好：

- 只开放 `POST /servermonitor/report`；
- 其他 Bot 能力不暴露；
- 上报接口只接收 JSON，不接收命令；
- 认证只认 token。

### ADR-4：为什么只存最新快照

监控图的核心诉求是“**现在**怎么样”，不是历史曲线。
所以：

- 默认只存每台服务器最新一份快照；
- 历史曲线不是本期核心；
- 需要趋势时再额外加聚合存储。

### ADR-5：为什么不把采集和渲染耦合

因为采集是高频、渲染是低频；
采集失败不应该拖垮渲染；
渲染器失败不应该影响数据上报。

所以两者必须解耦。

---

## 5. TRSS-Yunzai 已核实的源码事实

> 以下事实来自实际源码阅读，编码时应直接按这些行为来写。

### F1. 插件加载

- `lib/plugins/loader.js` 会扫描 `plugins/` 目录。
- 有 `index.js` 就整体加载；否则逐 `.js` 文件加载。
- 文件改动会触发热重载。

### F2. `rule` 与 `task`

- `lib/plugins/plugin.js` 支持 `rule`、`task`。
- `task` 可以是数组，`cron` 走 node-schedule。

### F3. 权限陷阱

`filtPermission()` 的真实行为是：

- `permission: "master"` → 非主人会被加载器自动拦截并回复“暂无权限，只有主人才能操作”；
- `permission: "admin" / "owner"` → **只在群聊里检查**，私聊会放行；
- 不设 `permission` → 所有人都能进 fnc。

所以：
- **查看类命令**不要靠 `permission: master` 去做自定义管理员；
- **管理类命令**才用 `permission: master`。

### F4. 渲染

- `e.runtime.render(plugin, path, data, cfg)` 会把模板渲染成图片。
- `cfg` 只管 `retType / recallMsg / beforeRender`；
- `imgType / quality / saveId` 这些是 **data** 的字段。
- `runtime.render` 的默认路径是 `plugins/<plugin>/resources/<path>.html`。

### F5. 多页不要依赖自动分页

`runtime.render` 的单数封装并不适合直接拿来做自动多页；
本项目采用**手动分块、分别渲染、数组回复**。

### F6. 回复数组是可行的

TRSS-Yunzai 示例里已经出现过数组回复，所以多图可以一次 `reply([seg1, seg2])`。

### F7. `e.runtime` 一定存在

加载器在派发插件前会初始化 runtime，所以 fnc 里可以直接用 `this.e.runtime`。

### F8. `Bot.express` 可用

Yunzai 内置 `express` 实例，已经带有：

- JSON / urlencoded / raw / text 解析；
- `serverAuth`；
- `quiet` / `skip_auth` 数组。

这就是本方案的服务端入口。

### F9. `Bot.sendMasterMsg` 可用

用于离线告警直接私发主人。

---

## 6. 统一数据契约

### 6.1 上报接口

```http
POST /servermonitor/report
Header: X-SM-Token: sm_xxxxxxxx
Content-Type: application/json
```

### 6.2 返回码

- `200`：成功
- `401`：token 不对
- `400`：格式不合法
- `422`：schema 不对

### 6.3 快照 JSON

```jsonc
{
  "v": 1,
  "name": "web-01",
  "agent_ts": 1700000000000,
  "os": {
    "platform": "linux",
    "distro": "Ubuntu 22.04",
    "release": "5.15.0",
    "arch": "x64",
    "hostname": "web-01",
    "uptime": 86400
  },
  "cpu": {
    "model": "Intel(R) Xeon(R) CPU E5-2696 v4",
    "cores": 44,
    "usage": 32.5,
    "temp": null,
    "power": null
  },
  "gpus": [
    {
      "model": "NVIDIA GeForce RTX 4090",
      "usage": 42,
      "temp": 63,
      "memUsed": 8.2,
      "memTotal": 24,
      "power": 180.5
    }
  ],
  "mem": {
    "used": 12.3,
    "total": 32,
    "swapUsed": 0.5,
    "swapTotal": 4
  },
  "net": {
    "iface": "eth0",
    "rxSec": 8.2,
    "txSec": 1.1,
    "rxTotal": 102400.5,
    "txTotal": 20480.2
  },
  "disks": [
    { "mount": "/", "used": 300.2, "total": 500 }
  ],
  "load": [2.3, 1.8, 1.6]
}
```

### 6.4 契约规则

1. 所有字段都允许 `null`，但**不允许缺失**。
2. 单位固定：GB / MB/s / ℃ / W / %。
3. `name` 最终以 Yunzai 注册表为准，agent 自报只能校验，不能覆盖。
4. `gpus=[]` 表示确认没有独显；`gpus=null` 表示采集失败。

---

## 7. 跨平台可得性矩阵

| 指标 | Linux | Windows | macOS |
|---|---|---|---|
| CPU 型号 / 核心 | ✅ | ✅ | ✅ |
| CPU 占用 | ✅ | ✅ | ✅ |
| CPU 温度 | ✅（lm-sensors / thermal zone） | ⚠️ 常取不到 | ⚠️ 常取不到 |
| GPU 型号 | ✅ | ✅ | ✅ |
| GPU 占用 / 温度 / 显存 / 功耗 | ✅（NVIDIA） | ✅（NVIDIA） | ⚠️ 很多场景不可得 |
| 内存 / 交换区 | ✅ | ✅ | ✅ |
| 网速 / 累计流量 | ✅ | ✅ | ✅ |
| 磁盘占用 / 容量 | ✅ | ✅ | ✅ |
| CPU 功耗 | ⚠️ RAPL 可用时可得 | ❌ 默认不可得 | ❌ 默认不可得 |
| 整机功耗 | ❌ 默认不可得 | ❌ 默认不可得 | ❌ 默认不可得 |
| 系统信息 / uptime | ✅ | ✅ | ✅ |

**结论**：
- Linux 场景最完整；
- Windows / macOS 要接受一些字段天然是 `—`；
- 这不是 bug，而是平台现实。

---

## 8. Bot 端设计

### 8.1 目录结构

```text
plugins/servermonitor/
├── index.js
├── model.js
├── server.js
├── config.js
├── resources/
│   ├── server_status.html
│   └── status.css
├── agent/
│   ├── agent.mjs
│   └── README.md
├── config.yaml
└── data/snapshots.json
```

### 8.2 路由注册

Yunzai 暴露一个专用上报接口：

```js
Bot.express.post("/servermonitor/report", handler)
```

建议同时做三件事：

1. `Bot.express.quiet.push("/servermonitor")`：静默健康上报日志；
2. `Bot.express.skip_auth.push("/servermonitor")`：让插件自己的 token 生效；
3. 路由注册加 flag：防止热重载重复注册。

### 8.3 命令设计

| 命令 | 权限 | 作用 |
|---|---|---|
| `#服务器状态` | 自定义管理员 | 总览图 |
| `#服务器状态 <名称>` | 自定义管理员 | 单机详情 |
| `#服务器状态列表` | 自定义管理员 | 列表 |
| `#服务器状态帮助` | 所有人 | 帮助 |
| `#服务器状态添加 <名称>` | 主人 | 添加服务器、生成 token |
| `#服务器状态删除 <名称>` | 主人 | 删除服务器 |

### 8.4 权限策略

- 查看类命令：**rule 不设 permission**，fnc 内判断 `e.isMaster || admins.includes(user_id)`。
- 管理类命令：`permission: "master"`。

这样既符合 TRSS-Yunzai 的真实行为，也避免权限陷阱。

### 8.5 离线判定

- `now - lastSeen > offline_timeout` → offline；
- `lastSeen` 用 **Yunzai 收包时间**，不要信 agent 时钟；
- 状态翻转时通知主人；
- 同一服务器加冷却时间，防抖。

### 8.6 数据存储

只存两样东西：

1. **注册表**：服务器名、token、备注；
2. **最新快照**：每台服务器最近一次上报。

默认不存秒级历史，避免无意义的存储膨胀。

---

## 9. Agent 端设计

### 9.1 形态

agent 是一个常驻进程，职责只有三个：

1. 采集本机指标；
2. 按间隔把 JSON 发到 Yunzai；
3. 失败时退避重试。

### 9.2 CLI

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxxxxxxx \
  --report-url http://<Yunzai地址>/servermonitor/report \
  --interval 10
```

可选：

- `--dry-run`：只采集一次并打印 JSON 后退出；
- `--fast-interval` / `--slow-interval`：支持分级采样；
- `--once`：做一次上报后退出，便于调试。

### 9.3 采样频率

建议分两层：

- **快指标**：CPU / 内存 / 网速，5～10 秒一次；
- **慢指标**：磁盘 / GPU / 温度 / 功耗，30 秒一次。

这样可以显著降低资源占用。

### 9.4 资源消耗

- CPU：通常很低；
- 内存：Node 常驻进程本身是最大头；
- 网络：每次只有几 KB JSON；
- 磁盘：默认不写盘。

**结论：主动上传本身不吃资源，真正吃的是“常驻进程”这部分。**

### 9.5 采集方法

- CPU / 内存 / 系统信息：`systeminformation`；
- GPU：`nvidia-smi` 优先；
- Linux 温度：`sensors` / thermal zone；
- CPU 功耗：RAPL（可用就取，不可用就 null）；
- 网速：采样差分；
- 磁盘：过滤虚拟文件系统后再统计。

### 9.6 常驻部署

- Linux：systemd；
- Windows：NSSM / 计划任务；
- macOS：launchd。

---

## 10. 渲染设计

### 10.1 总览图信息层级

第一眼必须看到：

1. 共几台；
2. 几台在线；
3. 哪些离线；
4. 哪些压力高。

### 10.2 布局建议

- 960px 左右宽度；
- 两列卡片；
- 每页 8 台左右；
- 离线卡片置灰；
- CPU / GPU / 内存 / 磁盘 / 网速 / 功耗用统一的条形区。

### 10.3 重要原则

- **模板只做展示，不做复杂计算**；
- 所有单位转换、空值处理、字符串拼接都在 `model.js` 做；
- 模板只打印 `cpuText / memText / netText / powerText` 这类最终字符串。

### 10.4 分页

不用依赖自动多页，直接：

1. 把服务器列表按 8 台一页切块；
2. 每页渲染成一张图；
3. 通过数组回复一次发出。

这样最稳，也最容易控制版式。

### 10.5 渲染失败降级

如果浏览器或模板出问题：

- 不要直接报错；
- 降级成文字版状态列表；
- 保证功能总能“看见结果”。

---

## 11. 安全设计

这是这套方案里最重要的一章。

### 11.1 只暴露专用接口

不要把整个 Bot 的管理能力暴露到公网。

只开放：

- `POST /servermonitor/report`

其他接口保持内网或只给 Bot 自己用。

### 11.2 每台服务器独立 token

- 每台机器一个 token；
- token 只允许上报状态；
- 不能上传别人的状态；
- 不能执行任何命令。

### 11.3 HTTPS

公网环境建议 HTTPS。

### 11.4 IP 白名单

如果服务器数量不多，最好在反向代理层做 IP 白名单。

### 11.5 日志静默

心跳接口不要刷日志，否则 5～10 秒一条会很烦。

### 11.6 管理命令私聊

`#服务器状态添加` 这类命令要在私聊里执行，避免 token 被群成员看到。

---

## 12. 配置参考

```yaml
admins: []
servers:
  - name: web-01
    token: sm_xxxxxxxx
    note: 主站
  - name: db-01
    token: sm_yyyyyyyy
    note: 数据库

page_size: 8
offline_timeout: 30
alert:
  enabled: true
  cooldown: 120
render:
  imgType: png
```

### 推荐默认值

- agent 上报间隔：**10s**；
- 离线阈值：**30s**；
- 页大小：**8 台**。

---

## 13. 里程碑

| 阶段 | 目标 | 验收 |
|---|---|---|
| M1 | 一台 Linux 真机闭环 | `#服务器状态` 出图，数据和实际一致 |
| M2 | 多台 Linux 机器 | 3～5 台并发上报，离线判定正常 |
| M3 | GPU / 温度 / 功耗 | 有独显机器字段正确，无独显机器正常降级 |
| M4 | Windows / macOS | 允许部分字段为 `—`，但整体不崩 |
| M5 | 安全和运维 | token、quiet、热重载、告警都正常 |

---

## 14. 测试计划

1. **采集测试**：agent `--dry-run` 输出 JSON，schema 校验通过。
2. **上报测试**：token 正确/错误、缺字段、脏字段、重复上报。
3. **渲染测试**：单机图、离线图、多机图、全 null 图。
4. **权限测试**：主人 / 管理员 / 普通用户 / 群聊 / 私聊全矩阵。
5. **压力测试**：10 秒一个周期，连续运行 24 小时，看有没有日志爆炸或内存泄漏。
6. **热重载测试**：改插件文件后，路由不能重复注册。

---

## 15. 风险与限制

1. **Yunzai 必须可达**：这是 push 模式的前提。
2. **常驻进程是固定成本**：Node agent 常驻内存是这套方案里最大的资源项。
3. **Windows / macOS 某些字段取不到很正常**：不要把 `—` 当 bug。
4. **长期历史曲线不是本期目标**：如果以后要做趋势图，建议单独加聚合存储。
5. **不要把 Bot 的其他接口暴露出去**：只暴露上报路径。

---

## 16. 附录：最小骨架代码

### 16.1 `server.js`

```js
import { handleReport } from "./model.js"

if (!Bot.express.__smReportRegistered) {
  Bot.express.__smReportRegistered = true
  if (!Bot.express.quiet.includes("/servermonitor")) Bot.express.quiet.push("/servermonitor")
  if (!Bot.express.skip_auth.includes("/servermonitor")) Bot.express.skip_auth.push("/servermonitor")

  Bot.express.post("/servermonitor/report", (req, res) => {
    return globalThis.__smReportHandler(req, res)
  })
}

globalThis.__smReportHandler = handleReport
```

### 16.2 `model.js`

```js
export function handleReport(req, res) {
  const token = req.get("X-SM-Token") || ""
  const body = req.body
  const server = findServerByToken(token)
  if (!server) return res.status(401).json({ ok: false, msg: "token invalid" })
  if (!body || body.v !== 1) return res.status(422).json({ ok: false, msg: "bad schema" })

  const snap = normalize(body, server.conf.name)
  store.set(server.conf.name, {
    conf: server.conf,
    snap,
    lastSeen: Date.now(),
    state: "online"
  })
  return res.json({ ok: true })
}
```

### 16.3 `index.js`

```js
import plugin from "../../lib/plugins/plugin.js"
import cfg from "./config.js"
import { allSnapshots, decorate, textFallback } from "./model.js"
import "./server.js"

export class serverMonitor extends plugin {
  constructor() {
    super({
      name: "servermonitor",
      dsc: "多服务器统一状态监控",
      event: "message",
      rule: [
        { reg: "^#?服务器状态帮助$", fnc: "help" },
        { reg: "^#?服务器状态列表$", fnc: "list" },
        { reg: "^#?服务器状态\\s+(\\S+)$", fnc: "statusOne" },
        { reg: "^#?服务器状态$", fnc: "statusAll" },
        { reg: "^#?服务器状态添加\\s+(\\S+)$", fnc: "add", permission: "master" },
        { reg: "^#?服务器状态删除\\s+(\\S+)$", fnc: "del", permission: "master" },
      ],
      task: [
        { name: "SM离线扫描", fnc: "scanOffline", cron: "*/10 * * * * *", log: false },
      ]
    })
  }

  isAdmin() {
    if (this.e.isMaster) return true
    return (cfg.read().admins || []).map(String).includes(String(this.e.user_id))
  }

  async statusAll() {
    if (!this.isAdmin()) return this.e.isGroup ? false : this.reply("权限不足")
    const snaps = allSnapshots()
    if (!snaps.length) return this.reply("尚未添加服务器")

    const pages = chunk(snaps.map(decorate), 8)
    const segs = []
    for (const [i, list] of pages.entries()) {
      const seg = await this.e.runtime.render("servermonitor", "server_status", {
        servers: list,
        pageNum: i + 1,
        pageCount: pages.length,
        updateTime: new Date().toLocaleString(),
        imgType: "png",
        saveId: `status_${Date.now()}_${i}`
      }, { retType: "base64" })
      if (seg) segs.push(seg)
    }
    if (!segs.length) return this.reply(textFallback(snaps))
    return this.reply(segs.length === 1 ? segs[0] : segs)
  }
}
```

### 16.4 `agent.mjs`

```js
// 只做三件事：采集、上报、退避重试
// 重点：区分快指标和慢指标，降低常驻成本
```

---

## 17. 参考资料

- TRSS-Yunzai：https://github.com/TimeRainStarSky/Yunzai
- TRSS-Yunzai 插件开发 Wiki：https://yunzai-bot.com/dev/
- TRSS-Yunzai 插件加载：https://yunzai-bot.com/dev/load.html
- komari 文档：https://www.komari.wiki/dev/agent
- nezha 架构文档：https://nezha.wiki/en_US/guide/architecture.html
- nezha NAT 文档：https://nezha.wiki/en_US/guide/nat.html
- ServerStatus 原理介绍：https://cloud.tencent.cn/developer/article/2324796?from=15425

---

*本版结论：主方案正式收敛为“Yunzai 暴露专用上报接口，agent 主动上传”。如果你下一步要落代码，我建议直接按这个版本开工。*
