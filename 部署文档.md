# servermonitor 部署文档

本文档说明如何部署 `servermonitor` TRSS-Yunzai 插件和服务器侧 agent。第一次安装建议先看 `快速部署教程.md`。

## 1. 架构

```text
服务器 agent 采集指标  ──POST /servermonitor/report──>  TRSS-Yunzai 插件  ──渲染图片──>  QQ
```

- Yunzai 端安装插件，负责收数、缓存、离线判定、渲染图片
- 服务器端安装 agent，负责采集 CPU / GPU / 内存 / 网络 / 磁盘 / 温度 / 功耗并主动上报
- 未添加服务器时，`#服务器状态` 默认显示 Yunzai 本机
- 添加服务器后，`#服务器状态` 显示 `本机 + 已注册服务器`

## 2. 统一交互式安装器

Linux / macOS：

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install.sh)
```

已 clone 仓库时：

```bash
./install.sh
```

Windows PowerShell：

```powershell
irm https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install.ps1 | iex
```

已 clone 仓库时：

```powershell
.\install.ps1
```

打开后可以选择：

1. 安装/更新 Yunzai 插件
2. 按当前系统安装 agent
3. Linux systemd agent
4. Docker Compose agent
5. Windows NSSM agent
6. macOS launchd agent
7. 只生成命令

安装 agent 时会让你输入 Yunzai 机器 IP/域名、端口和上报路径。

### 2.1 2536 端口说明

`2536` 通常是 TRSS-Yunzai 的 HTTP 服务端口。`servermonitor` 不新开端口，只复用 `Bot.express`，新增路径：

```text
POST /servermonitor/report
```

所以它和 OneBotV11 同端口时，是同一个 HTTP 服务里的不同路径；agent 访问 `http://YUNZAI_IP:2536/servermonitor/report` 即可。

## 3. Yunzai 插件安装

### 3.1 一键安装

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-plugin.sh) /path/to/Yunzai
```

### 3.2 手动安装

```bash
cd /path/to/Yunzai
rm -rf plugins/servermonitor
git clone https://github.com/qsbb/servermonitor.git plugins/servermonitor
ls plugins/servermonitor/index.js
```

正确入口：

```text
plugins/servermonitor/index.js
```

如果是下面这种路径，插件不会被 TRSS-Yunzai 正常加载：

```text
plugins/servermonitor/servermonitor/index.js
```

### 3.3 重启 Yunzai

```bash
cd /path/to/Yunzai
pnpm start
```

如果你用 systemd / pm2 / screen / docker 管理 Yunzai，请按原方式重启。

### 3.4 自检

QQ 发送：

```text
#服务器状态检查
```

正常会返回：

```text
【servermonitor】自检
插件加载：正常
HTTP接口：已注册
...
```

再发送：

```text
#服务器状态
```

未添加远程服务器时会显示 `本机` 状态。

## 4. 配置文件

位置：

```text
plugins/servermonitor/config.yaml
```

默认内容：

```yaml
admins: []
servers: []
shared_token: "sm_xxx"
page_size: 8
offline_timeout: 30
public_status: true
include_local: true
alert:
  enabled: true
  cooldown: 120
render:
  imgType: png
show_ip_in_image: false
```

字段：

| 字段 | 说明 |
| --- | --- |
| `admins` | 可查看状态的额外用户 ID，master 默认有权限 |
| `servers` | 已注册服务器列表，由 `#服务器状态添加` / `#服务器状态绑定` 自动维护 |
| `shared_token` | 共享上报 token；首次启动自动生成，可用 `#服务器状态令牌` 查看 |
| `page_size` | 每张状态图显示服务器数量 |
| `offline_timeout` | 超过多少秒未上报判定离线 |
| `public_status` | `true` 时任何人可查看状态；`false` 时仅 master/admins 可查看 |
| `include_local` | `true` 时总览包含 Yunzai 本机卡片 |
| `alert.enabled` | 是否启用离线/恢复提醒 |
| `alert.cooldown` | 告警冷却时间，秒 |
| `render.imgType` | 图片格式，`png` / `jpeg` |

## 5. 上报 token 与服务器绑定

上报 token 是 agent 上传时放在请求头 `X-SM-Token` 里的密钥。

### 5.1 推荐流程：服务器生成 token，Yunzai 接受绑定

在被监控机器运行部署脚本时，token 参数可以留空；脚本会自动生成 token，并输出：

```text
[servermonitor-agent] token: sm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
[servermonitor-agent] bind in Yunzai private chat: #服务器状态绑定 sm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

看到一次上传日志后，复制绑定命令，回到 Yunzai 主人私聊发送：

```text
#服务器状态绑定 sm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

绑定时服务器名称会自动取子服务器上报的 `SM_NAME` / `--name`。绑定完成后，该机器下一次上报就会显示在 `#服务器状态` 里；后续可用 `#服务器状态改名 <旧名> <新名>` 修改显示名称。如果提示还没收到该 token 的上报，等 agent 输出一次 uploaded 日志后再绑定。

### 5.2 可选流程：Yunzai 生成共享 token

主人私聊机器人：

```text
#服务器状态令牌
```

所有服务器都可以使用这个共享 token；第一次上报时会按机器名自动注册。

### 5.3 可选流程：Yunzai 生成单机 token

主人私聊机器人：

```text
#服务器状态添加 web-01
```

机器人会返回独立 token、上报接口和 agent 启动命令。

## 5.5 GitHub 克隆自动镜像加速

所有一键脚本克隆源码时都会先用 `git ls-remote` 探测 GitHub 加速节点，然后选择响应最快的地址克隆；单次克隆失败会自动重试。

一键脚本还会检测已安装的 agent：

| 平台 | 检测位置 | 更新方式 |
| --- | --- | --- |
| Linux systemd | `/etc/systemd/system/servermonitor-agent.service` | 读取旧环境变量，更新代码后 `daemon-reload` 并重启 |
| Linux Docker | `/opt/servermonitor-docker/.env` | 保留 `.env`，更新源码后 `docker compose up -d --build` |
| Windows NSSM | `C:\servermonitor\agent` + NSSM 服务 | 读取旧 `AppEnvironmentExtra`，更新代码并重启服务 |
| macOS launchd | `/Library/LaunchDaemons/com.servermonitor.agent.plist` | 读取旧环境变量，更新代码后重新 bootstrap |

更新时不需要重新传 token；直接重跑同一条一键命令即可。交互安装器也会在进入参数询问前检测已有安装，并提供「更新并保留现有配置」选项。

默认候选：

```text
https://github.com/qsbb/servermonitor.git
https://ghfast.top/https://github.com/qsbb/servermonitor.git
https://gh-proxy.com/https://github.com/qsbb/servermonitor.git
https://gitclone.com/github.com/qsbb/servermonitor.git
https://mirror.ghproxy.com/https://github.com/qsbb/servermonitor.git
```

相关变量：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `AUTO_GIT_MIRROR` | `1` | 是否自动测速 GitHub 镜像 |
| `REPO_MIRRORS` | 内置候选列表 | 逗号分隔的仓库克隆地址 |
| `REPO_URL` | 空 | 手动指定仓库地址，优先级最高，设置后跳过测速 |
| `GIT_MIRROR_PROBE_TIMEOUT` | `5` | 单个镜像探测超时秒数 |
| `GIT_CLONE_ATTEMPTS` | `3` | 克隆重试次数 |
| `GIT_CLONE_TIMEOUT` | `300` | 单次克隆超时秒数 |
| `BRANCH` | `main` | 克隆分支 |

示例：

```bash
REPO_MIRRORS="https://github.com/qsbb/servermonitor.git,https://ghfast.top/https://github.com/qsbb/servermonitor.git" \
  sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-linux.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

关闭自动测速：

```bash
AUTO_GIT_MIRROR=0 bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install.sh)
```

## 6. Linux agent 一键部署：systemd

在被监控 Linux 服务器执行：

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-linux.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

脚本会自动：

- clone 仓库
- 复制 `agent/` 到 `/opt/servermonitor/agent`
- 执行 `npm install --omit=dev`
- 生成 `/etc/systemd/system/servermonitor-agent.service`
- 开机自启并立即启动

查看状态：

```bash
systemctl status servermonitor-agent
```

查看日志：

```bash
journalctl -u servermonitor-agent -f
```

## 7. Docker agent 部署

### 7.1 Docker 一键部署

在被监控 Linux 服务器执行：

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

脚本会自动：

- clone 仓库到 `/opt/servermonitor-docker`
- 写入 `.env`
- 自动测速 Node 基础镜像节点并选择最快的 `NODE_IMAGE`
- 构建 `servermonitor-agent:latest`
- 使用 `docker compose` 启动容器

查看日志：

```bash
cd /opt/servermonitor-docker
docker compose -f docker-compose.agent.yml logs -f
```

停止：

```bash
cd /opt/servermonitor-docker
docker compose -f docker-compose.agent.yml down
```

### 7.2 Docker 镜像节点测速与择优

Docker 一键脚本会测试多个 Node 基础镜像地址的 manifest 响应速度，然后把最快的镜像写入 `.env`：

```dotenv
NODE_IMAGE=docker.1ms.run/library/node:18-bookworm-slim
```

默认候选列表：

```text
node:18-bookworm-slim
docker.1ms.run/library/node:18-bookworm-slim
docker.m.daocloud.io/library/node:18-bookworm-slim
hub.rat.dev/library/node:18-bookworm-slim
```

可手动指定候选列表：

```bash
NODE_IMAGE_CANDIDATES="node:18-bookworm-slim,docker.1ms.run/library/node:18-bookworm-slim,docker.m.daocloud.io/library/node:18-bookworm-slim" \
  sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

可直接指定基础镜像，跳过测速结果：

```bash
NODE_IMAGE=node:18-bookworm-slim \
  sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

关闭自动测速：

```bash
AUTO_NODE_IMAGE=0 sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-docker.sh) \
  web-01 \
  http://192.168.1.10:2536/servermonitor/report
```

相关变量：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `AUTO_NODE_IMAGE` | `1` | 是否自动测速选择基础镜像 |
| `NODE_IMAGE_CANDIDATES` | 内置候选列表 | 逗号分隔的镜像候选 |
| `NODE_IMAGE_PROBE_TIMEOUT` | `8` | 单个候选测速超时秒数 |
| `NODE_IMAGE_PRE_PULL` | `1` | 构建前是否先 `docker pull` 选中的镜像 |
| `NODE_IMAGE` | 空 | 手动指定基础镜像，优先级最高 |

### 7.3 Docker Compose 手动部署

```bash
mkdir -p /opt/servermonitor-docker
cd /opt/servermonitor-docker
git clone https://github.com/qsbb/servermonitor.git .
cp .env.agent.example .env
```

编辑 `.env`：

```dotenv
SM_NAME=web-01
SM_TOKEN=sm_xxx
SM_REPORT_URL=http://192.168.1.10:2536/servermonitor/report
SM_INTERVAL=10
SM_SLOW_INTERVAL=30
SM_TIMEOUT=5000
NODE_IMAGE=node:18-bookworm-slim
```

启动：

```bash
docker compose --env-file .env -f docker-compose.agent.yml up -d --build
```

### 7.4 Docker 指标说明

`docker-compose.agent.yml` 默认启用：

```yaml
network_mode: host
pid: host
volumes:
  - /sys:/sys:ro
```

NVIDIA GPU 场景：

1. 安装 `nvidia-container-toolkit`
2. 在 `docker-compose.agent.yml` 中启用：

```yaml
gpus: all
```

宿主机指标最完整的方式是 Linux systemd 原生部署；Docker 适合统一容器化管理。

## 8. Linux agent 手动部署

```bash
mkdir -p /opt/servermonitor
cd /tmp
rm -rf servermonitor-src
git clone https://github.com/qsbb/servermonitor.git servermonitor-src
cp -r /tmp/servermonitor-src/agent /opt/servermonitor/agent
cd /opt/servermonitor/agent
npm install --omit=dev
```

测试采集：

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxx \
  --report-url http://192.168.1.10:2536/servermonitor/report \
  --dry-run
```

上传一次：

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxx \
  --report-url http://192.168.1.10:2536/servermonitor/report \
  --once
```

持续运行：

```bash
node agent.mjs \
  --name web-01 \
  --token sm_xxx \
  --report-url http://192.168.1.10:2536/servermonitor/report \
  --interval 10 \
  --slow-interval 30
```

## 9. Windows agent 部署

### 9.1 一键部署

前置条件：

- Windows 已安装 Node.js 18+
- Windows 已安装 Git
- 使用管理员 PowerShell

管理员 PowerShell 执行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process -Force
irm https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-windows.ps1 -OutFile $env:TEMP\install-agent-windows.ps1
powershell -ExecutionPolicy Bypass -File $env:TEMP\install-agent-windows.ps1 `
  -Name "win-01" `
  -ReportUrl "http://192.168.1.10:2536/servermonitor/report"
```

脚本会自动：

- clone 仓库
- 复制 agent 到 `C:\servermonitor\agent`
- 安装 npm 依赖
- 下载或复用 NSSM
- 注册 Windows 服务 `servermonitor-agent`
- 写入环境变量并启动服务

查看状态：

```powershell
nssm status servermonitor-agent
```

查看日志：

```powershell
Get-Content C:\servermonitor\agent.log -Wait
Get-Content C:\servermonitor\agent.err.log -Wait
```

### 9.2 手动部署

```powershell
git clone https://github.com/qsbb/servermonitor.git C:\servermonitor\src
Copy-Item -Recurse -Force C:\servermonitor\src\agent C:\servermonitor\agent
cd C:\servermonitor\agent
npm install --omit=dev
node agent.mjs --name win-01 --token sm_xxx --report-url http://192.168.1.10:2536/servermonitor/report --once
```

注册服务：

```powershell
nssm install servermonitor-agent "C:\Program Files\nodejs\node.exe" "C:\servermonitor\agent\agent.mjs"
nssm set servermonitor-agent AppDirectory "C:\servermonitor\agent"
nssm set servermonitor-agent AppEnvironmentExtra `
  "SM_NAME=win-01" `
  "SM_TOKEN=sm_xxx" `
  "SM_REPORT_URL=http://192.168.1.10:2536/servermonitor/report" `
  "SM_INTERVAL=10" `
  "SM_SLOW_INTERVAL=30"
nssm start servermonitor-agent
```

停止 / 删除：

```powershell
nssm stop servermonitor-agent
nssm remove servermonitor-agent confirm
```

## 10. macOS agent 部署

### 10.1 一键部署

前置条件：

- macOS 已安装 Node.js 18+
- macOS 已安装 Git

执行：

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/qsbb/servermonitor/main/scripts/install-agent-macos.sh) \
  mac-01 \
  http://192.168.1.10:2536/servermonitor/report
```

脚本会自动：

- clone 仓库
- 复制 agent 到 `/opt/servermonitor/agent`
- 安装 npm 依赖
- 生成 `/Library/LaunchDaemons/com.servermonitor.agent.plist`
- 设置开机自启并启动

查看状态：

```bash
launchctl print system/com.servermonitor.agent
```

查看日志：

```bash
tail -f /var/log/servermonitor/agent.log /var/log/servermonitor/agent.err.log
```

### 10.2 手动部署

```bash
sudo mkdir -p /opt/servermonitor
cd /tmp
rm -rf servermonitor-src
git clone https://github.com/qsbb/servermonitor.git servermonitor-src
sudo cp -r /tmp/servermonitor-src/agent /opt/servermonitor/agent
cd /opt/servermonitor/agent
sudo npm install --omit=dev
node agent.mjs --name mac-01 --token sm_xxx --report-url http://192.168.1.10:2536/servermonitor/report --once
```

launchd 常驻建议使用一键脚本生成 plist。

停止 / 卸载：

```bash
sudo launchctl bootout system /Library/LaunchDaemons/com.servermonitor.agent.plist
sudo rm -f /Library/LaunchDaemons/com.servermonitor.agent.plist
sudo rm -rf /opt/servermonitor/agent
```

## 11. agent 参数和环境变量

命令参数：

| 参数 | 说明 |
| --- | --- |
| `--name` | 服务器名，建议与 Yunzai 注册名一致 |
| `--token` | 上报 token，可由 Yunzai 生成，也可由部署脚本在服务器侧生成后再绑定 |
| `--report-url` | Yunzai 上报接口 |
| `--interval` | 快速采集与上传间隔，默认 10 秒 |
| `--slow-interval` | GPU/磁盘/温度等慢指标刷新间隔，默认 30 秒 |
| `--timeout` | HTTP 上传超时，默认 5000 毫秒 |
| `--dry-run` | 仅输出采集快照，不上传 |
| `--once` | 上传一次后退出 |

环境变量：

| 环境变量 | 对应参数 |
| --- | --- |
| `SM_NAME` | `--name` |
| `SM_TOKEN` | `--token` |
| `SM_REPORT_URL` | `--report-url` |
| `SM_INTERVAL` | `--interval` |
| `SM_SLOW_INTERVAL` | `--slow-interval` |
| `SM_TIMEOUT` | `--timeout` |
| `SM_DRY_RUN` | `--dry-run` |
| `SM_ONCE` | `--once` |

## 12. 验证

查看上传日志：

```text
[servermonitor-agent] web-01 uploaded @ 2026-01-01T00:00:00.000Z
```

QQ 发送：

```text
#服务器状态
```

查看列表：

```text
#服务器状态列表
```

查看单台：

```text
#服务器状态 web-01
```

## 13. 反向代理示例

```nginx
location /servermonitor/ {
  proxy_pass http://127.0.0.1:2536/servermonitor/;
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

## 14. 常见问题

### 14.1 发命令没反应

先检查插件入口：

```bash
ls /path/to/Yunzai/plugins/servermonitor/index.js
```

再发自检：

```text
#服务器状态检查
```

群里无响应时，私聊测试或 at 机器人：

```text
@机器人 #服务器状态检查
```

### 14.2 显示“未上报”

检查：

- agent 是否运行
- `SM_REPORT_URL` / `--report-url` 是否能从目标服务器访问
- token 是否复制正确
- Yunzai HTTP 端口是否放行
- `offline_timeout` 是否大于 agent `SM_INTERVAL`

### 14.3 GPU 数据为空

- NVIDIA GPU 需要 `nvidia-smi`
- Docker 需要 NVIDIA runtime 并启用 `gpus: all`
- 非 NVIDIA GPU 通常只能展示型号

### 14.4 温度或功耗为空

- CPU 温度依赖系统传感器
- Linux CPU 功耗优先读取 Intel RAPL
- GPU 功耗优先读取 `nvidia-smi`
- Windows / macOS 部分硬件指标由系统能力决定

## 15. 升级

```bash
cd /path/to/Yunzai
cp plugins/servermonitor/config.yaml /tmp/servermonitor.config.yaml 2>/dev/null || true
cp -r plugins/servermonitor/data /tmp/servermonitor.data 2>/dev/null || true
rm -rf plugins/servermonitor
git clone https://github.com/qsbb/servermonitor.git plugins/servermonitor
cp /tmp/servermonitor.config.yaml plugins/servermonitor/config.yaml 2>/dev/null || true
rm -rf plugins/servermonitor/data
cp -r /tmp/servermonitor.data plugins/servermonitor/data 2>/dev/null || mkdir -p plugins/servermonitor/data
```

重启 Yunzai 后发送：

```text
#服务器状态检查
```

## 16. 卸载

```bash
rm -rf /path/to/Yunzai/plugins/servermonitor
```
