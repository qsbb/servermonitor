{
  "name": "servermonitor-agent",
  "private": true,
  "type": "module",
  "version": "0.1.20",
  "description": "servermonitor agent for host metric reporting",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node agent.mjs",
    "dry-run": "node agent.mjs --dry-run",
    "build:exe": "node build-exe.mjs"
  },
  "bin": "agent.mjs",
  "pkg": {
    "targets": [
      "node18-win-x64"
    ],
    "outputPath": "../dist"
  },
  "dependencies": {
    "systeminformation": "^5.24.0"
  },
  "devDependencies": {
    "esbuild": "0.21.5",
    "pkg": "5.8.1"
  }
}
